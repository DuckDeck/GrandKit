    import XCTest
    @testable import GrandKit

    final class GrandKitTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(GrandKit().text, "Hello, World!")
        }
    }
